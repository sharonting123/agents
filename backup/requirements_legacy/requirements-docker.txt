# Docker 内 pip 安装（基座镜像已含 PyTorch CUDA；版本可按本机 CUDA 调整）
# 构建：在 Code 目录 docker build -t procurement-qa .
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0
pymysql>=1.1
loguru>=0.7
transformers>=4.36
accelerate>=0.25
sentencepiece
protobuf
safetensors
# F 类检索等按需取消注释：
# langchain-core
# langchain-community
# faiss-cpu
